# Скопіюйте цей файл у .env та підставте реальні дані з MongoDB Atlas:
# Connect → Drivers → Copy connection string.
MONGO_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/?retryWrites=true&w=majority
